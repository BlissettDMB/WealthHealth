@model WealthHealth.ViewModels.Investments.MutualFunds.EditMutualFundVm

@{
    ViewBag.Title = "Edit MutualFund";
}

<hgroup class="title">
    <h1>Mutual Fund.</h1>
    <h2>Edit</h2>
</hgroup>
@using (Html.BeginForm("Edit", "MutualFunds", new { area = "", id = Model.Id  }, FormMethod.Post, new {  @class = "form-horizontal" }))
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()
    <fieldset>
        <legend>Edit Mutual Fund</legend>
        
        @Html.HiddenFor(model => model.Id)

        <div class="control-group">
            @Html.LabelFor(model => model.Title, new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(model => model.Title, new { @class = "input-xlarge" })
                @Html.ValidationMessageFor(model => model.Title)
            </div>
        </div>
        
        <div class="control-group">
            @Html.LabelFor(model => model.Symbol, new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(model => model.Symbol, new { @class = "input-xlarge" })
                @Html.ValidationMessageFor(model => model.Symbol)
            </div>
        </div>

        <div class="form-actions">
            <input type="submit" class="btn btn-primary btn-large" value="Update Mutual Fund" />
            @Html.ActionLink("Cancel", "Index", "MutualFunds", new { area = "" }, new { @class = "btn btn-large" })
        </div>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}