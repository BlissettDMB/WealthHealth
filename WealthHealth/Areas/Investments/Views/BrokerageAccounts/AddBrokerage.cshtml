@model WealthHealth.ViewModels.Investments.BrokerageAccounts.AddBrokerageVm

@{
    ViewBag.Title = "Add Brokerage Account";
}

<hgroup class="title">
    <h1>Brokerage.</h1>
    <h2>Create</h2>
</hgroup>
@using (Html.BeginForm("AddBrokerage", "BrokerageAccounts", new { area = ""  }, FormMethod.Post, new {  @class = "form-horizontal" }))
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()
    <fieldset>
        <legend>Add Brokerage</legend>
        
        <div class="control-group">
            @Html.LabelFor(model => model.Title, new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(model => model.Title, new { @class = "input-xlarge" })
                @Html.ValidationMessageFor(model => model.Title)
            </div>
        </div>
        
        <div class="form-actions">
            <input type="submit" class="btn btn-primary btn-large" value="Add Brokerage" />
            @Html.ActionLink("Cancel", "Index", "BrokerageAccounts", new { area = "" }, new { @class = "btn btn-large" })
        </div>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
